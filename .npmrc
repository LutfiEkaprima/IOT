# Use local node_modules for all installations
prefix=${HOME}/.npm-global
# Avoid permission issues with global installs
unsafe-perm=true
# Use exact versions to avoid conflicts
save-exact=true
# Disable package-lock for cleaner installs in WebContainer
package-lock=false